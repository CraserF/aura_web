// TODO(phase-3): Encode the deterministic compaction strategy so source
// selection, UI reporting, and run warnings stay in sync.
export function resolveCompactionPolicy(): string {
  return 'unconfigured';
}
