import type { AssembleContextInput } from '@/services/context/assemble';

// TODO(phase-3): Split source selection from packaging so context controls can
// be inspected and adjusted before compaction is applied.
export function selectContextSources(_input: AssembleContextInput): void {
}
