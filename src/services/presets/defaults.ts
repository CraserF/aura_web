import { defaultWorkflowPresets } from '@/services/projectRules/defaults';

export const DEFAULT_PRESET_STATE = defaultWorkflowPresets();

// TODO(phase-8): Move preset defaults behind the new service layer.
