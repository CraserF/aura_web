import type { ProjectGraph, ProjectGraphIssue } from './types';

// TODO(phase-6): Validate graph edges and surface broken/stale references.
export function validateProjectGraph(_graph: ProjectGraph): ProjectGraphIssue[] {
  return [];
}
